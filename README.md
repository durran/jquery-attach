This is a work in progress, just a spike for now.

Development Setup
=================
`gem install bundler`
`bundle install`

Running the Example
===================
`rackup` and go to http://localhost:9292

Running Specs
=============
`evergreen serve`
